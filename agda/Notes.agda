
-- module Notes where

-- data A : Set
-- data B : (A → A) → Set

-- data A where
--   a : A
--   b : ∀ f → B f → A

-- data B where
--   b : B (λ _ → a)

-- indA : (Aᴰ : A → Set)(Bᴰ : ∀ f → B f → Set)(aᴰ : Aᴰ a)(bᴰ : Bᴰ _ b) → {!!}
-- indB : (Aᴰ : A → Set)(Bᴰ : ∀ f → B f → Set)(aᴰ : Aᴰ a)(bᴰ : Bᴰ _ b) → {!!}
-- indA = {!!}
-- indB = {!!}
